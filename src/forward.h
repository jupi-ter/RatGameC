#ifndef FORWARD_H
#define FORWARD_H

// Forward declarations for all major types
// Include this header to break circular dependencies

typedef struct EntityRegistry EntityRegistry;
typedef struct TransformArray TransformArray;
typedef struct RenderableArray RenderableArray;
typedef struct CircleArray CircleArray;
typedef struct RectangleArray RectangleArray;
typedef struct GameState GameState;

#endif
